{
  "devDependencies": {
    "jest": "^27.0.6"
  },
  "scripts": {
    "test": "jest --watchAll --no-cache"
  },
  "jest": {
    "testEnvironment": "node",
    "setupFilesAfterEnv": [
      "./app.test.js"
    ]
  }
}

